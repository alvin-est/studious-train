export default class Department {
    constructor(name) {
        this.name = name;
    }

    // Getters
    getName() {
        return this.name;
    }

    getAll() {
        
    }

    // Setters
    setName(newName) {
        this.name = newName;
    }
}